enum Color { red, green, blue }
fn next(c: Color) Color {
    if (c == Color.red) { return Color.green; }
    if (c == Color.green) { return Color.blue; }
    return Color.red;
}
fn main() i32 {
    let c: Color = next(next(Color.red));
    if (c == Color.blue) { return 42; }
    return 0;
}
