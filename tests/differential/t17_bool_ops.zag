fn side(c: *i32) bool { c.* = c.* + 1; return true; }
fn main() i32 {
    let n: i32 = 0;
    let x: bool = false && side(&n);   // short-circuit: no side effect
    let y: bool = true || side(&n);    // short-circuit: no side effect
    if (!x && y && n == 0) { return 42; }
    return n;
}
