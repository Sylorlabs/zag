struct P { a: i32, b: i32 }
fn f(n: i32) P {
    if (n == 0) { return P{ .a = 0, .b = 1 }; }
    let q: P = f(n - 1);
    return P{ .a = q.a + q.b, .b = q.a };
}
fn main() i32 {
    let r: P = f(10);
    return r.a - 13;    // fib-ish: a=55 → 42
}
