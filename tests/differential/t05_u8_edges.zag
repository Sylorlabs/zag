fn main() i32 {
    
    let b: *u8 = _zag_malloc(4);
    b[0] = 255; b[1] = 0;
    let x: i32 = (b[0] as i32) + (b[1] as i32);   // 255
    b[0] = (300 % 256) as u8;                      // 44
    return x - 255 + (b[0] as i32) - 2;            // 42
}
