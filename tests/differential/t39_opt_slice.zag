fn pick(k: i32) ?[]u8 { if (k == 1) { return "hello"; } return null; }
fn main() i32 {
    let a: ?[]u8 = pick(1);
    let b: ?[]u8 = pick(0);
    let n: i32 = 0;
    if (a) |s| { n = n + s.len; }
    if (b) |s| { n = n + 100; }
    return n + 37;
}
