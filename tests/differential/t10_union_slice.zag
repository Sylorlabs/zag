union V { s: []u8, n: i32 }
fn pick(v: V) i32 {
    switch (v) { .s => |t| { return t.len; } .n => |m| { return m; } }
    return 0;
}
fn main() i32 {
    let a: V = V{ .s = "hello" };
    let b: V = V{ .n = 37 };
    return pick(a) + pick(b);
}
