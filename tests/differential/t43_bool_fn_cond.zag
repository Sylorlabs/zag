fn is_even(x: i32) bool { return x % 2 == 0; }
fn main() i32 {
    let n: i32 = 0;
    let i: i32 = 0;
    while (i < 20) { if (is_even(i) && !is_even(i + 1) == false) { n = n + 100; } else if (is_even(i)) { n = n + 4; } i = i + 1; }
    return n + 2;   // 10 evens * 4 + 2 = 42
}
