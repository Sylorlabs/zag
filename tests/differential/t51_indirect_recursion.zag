fn step(n: i32, self: fn(i32, fn(i32, i32) i32) i32) i32 { return n; }
fn count(n: i32) i32 {
    let dec: fn(i32) i32 = fn[](x: i32) i32 { return x - 1; };
    let s: i32 = 0;
    let i: i32 = n;
    while (i > 0) { s = s + 1; i = dec(i); }
    return s;
}
fn main() i32 { return count(42); }
