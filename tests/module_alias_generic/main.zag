@import("alias_first.zag")
@import("flat_second.zag")
@import("../../std/list.zag") as list

fn main() i32 {
    let core: Core = make_core();
    let event: *Event = list.get[*Event](core.events, 1);
    return event.*.value;
}
