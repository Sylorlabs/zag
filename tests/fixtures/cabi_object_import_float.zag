// Floats are deliberately outside the first direct-object import ABI slice.
extern fn cabi_scale(x: f64) f64 @cabi;

pub fn bad_object_import(x: f64) f64 @cabi_export {
    unsafe { return cabi_scale(x); }
}
