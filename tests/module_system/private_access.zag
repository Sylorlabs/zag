// private_access.zag — attempts to access a private symbol from a module.
// This should fail: `internal` is not pub in utils.zag.
@import("utils.zag") as utils

fn main() i32 {
    return utils.internal();
}
