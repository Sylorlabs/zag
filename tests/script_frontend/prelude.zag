script;

let path: []u8 = "/tmp/zag-script-prelude.txt";
if (write_file(path, "file-data") != 0) { return 21; }
let data: []u8 = read_file(path);
if (data.len != 9) { return 22; }
print("prelude:");
println(data);
return 0;
