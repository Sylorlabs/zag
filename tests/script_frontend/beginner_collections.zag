script;

let numbers = list(2, 3, 5);
if (append(numbers, 7) != 0) { return 1; }
if (length(numbers) != 4) { return 2; }
if (item_at(numbers, 0) != 2 || item_at(numbers, 3) != 7) { return 3; }
println(length(numbers));
return 0;
