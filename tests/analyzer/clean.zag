// No findings: one allocation is freed, the other is returned (ownership moves).
fn freed() void {
    let buf: []f32 = zalloc(4);
    buf[0] = 2.0;
    zfree(buf);
}
fn moved() []f32 {
    let out: []f32 = zalloc(4);
    return out;
}
fn arith(x: i32) i32 {
    return x + 1;
}
