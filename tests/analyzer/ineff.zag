// E0101 / E0102: algebraic identities and always-constant subexpressions.
fn compute(x: i32) i32 {
    let a: i32 = x + 0;      // E0101
    let b: i32 = a * 1;      // E0101
    let c: i32 = b * 0;      // E0102
    let d: i32 = x - x;      // E0102
    return a - c + d;
}
