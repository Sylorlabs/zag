struct Pair { left:i32, right:usize }

fn total(pair:Pair, bias:i32) i32 {
    return pair.left + (pair.right as i32) + bias;
}

fn main() i32 {
    let pair:Pair=Pair{.left=19,.right=20};
    return total(pair,3);
}
