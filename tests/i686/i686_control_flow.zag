fn main() i32 {
    let i: i32 = 0;
    let total: i32 = 0;
    while (i < 10) {
        i = i + 1;
        if (i == 3) { continue; }
        if (i > 7) { break; }
        total = total + i;
    }
    if (total != 25) { return 1; } else { return 42; }
}
