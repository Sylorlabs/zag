@import("../selfhost/std/process.zag") as process

fn main() i32 {
    let pair:*i64=process.process_alloc(null as *opaque,0,8) as *i64;
    if(pair==null as *i64){return 1;}
    pair[0]=42;
    if(pair[0]!=42){return 2;}
    return 42;
}
