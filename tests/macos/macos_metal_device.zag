// Native framework-binding smoke test.  The full Metal execution gate builds
// on this device acquisition before creating a queue, compiling a library, and
// reading back a buffer.
extern fn MTLCreateSystemDefaultDevice() *u8

fn main() i32 {
    let device:*u8=MTLCreateSystemDefaultDevice();
    if(device==null as *u8){return 1;}
    return 42;
}
