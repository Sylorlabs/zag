@import("../selfhost/std/std.zag")

fn main() i32 {
    if (io_write_file("/tmp/zag_process_after_io.txt","native") != 0) { return 1; }
    let v: []u8 = io_read_file("/tmp/zag_process_after_io.txt");
    if (!@strEq(v,"native")) { return 2; }
    return process_run_shell("exit 7");
}
