@import("../selfhost/native/hot_rt_macos.zag")

fn main() i32 {
    // The test exercises the Mach-O runtime's no-patch path; the release gate
    // stages a replacement while this loop is live.
    if (zag_hot_should_continue()) { return 42; }
    return 1;
}
