// A first Zag Script program: no main function, type annotations, pointers,
// allocator arguments, or imports are required for this small program.
script;

let numbers = list(2, 3, 5);
if (append(numbers, 7) != 0) { return 1; }

println("How many numbers?");
println(length(numbers));
println("The first number is:");
println(item_at(numbers, 0));

return 0;
