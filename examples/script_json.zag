@import("std:json") as json

script;

let encoded: []u8 = json.json_stringify_string("Zag Script");
println(encoded);
let decoded: json.JsonStringResult = json.json_parse_string(encoded);
if (decoded.ok == 0) { return 1; }
println(decoded.value);
return 0;
