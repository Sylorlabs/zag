script;

let result = process_run_timeout("printf 'bounded Zag process'", 1000, 4096);
if (process_result_state(result) != process_state_exited()) {
    println("process did not exit normally");
    return 1;
}
print(process_result_output(result));
return process_result_status(result);
