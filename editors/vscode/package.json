{
  "name": "zag-lang",
  "displayName": "Zag Language",
  "description": "Zag language support: syntax highlighting, diagnostics, completion, hover, and go-to-definition via the Zag LSP server.",
  "version": "0.1.0",
  "publisher": "sylorlabs",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Sylorlabs/zag.git",
    "directory": "editors/vscode"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "activationEvents": [
    "onLanguage:zag"
  ],
  "main": "./out/extension.js",
  "files": [
    "out/extension.js",
    "language-configuration.json",
    "syntaxes",
    "README.md",
    "LICENSE.txt"
  ],
  "contributes": {
    "languages": [
      {
        "id": "zag",
        "aliases": ["Zag", "zag"],
        "extensions": [".zag"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "zag",
        "scopeName": "source.zag",
        "path": "./syntaxes/zag.tmLanguage.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Zag",
      "properties": {
        "zag.serverPath": {
          "type": "string",
          "default": "zag-lsp",
          "description": "Path to the Zag LSP server binary."
        },
        "zag.trace.server": {
          "type": "string",
          "enum": ["off", "messages", "verbose"],
          "default": "off",
          "description": "Trace LSP communication in the Output panel."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./ --noEmit && esbuild extension.ts --bundle --minify --external:vscode --platform=node --format=cjs --outfile=out/extension.js",
    "package": "vsce package --out zag-lang.vsix",
    "install:local": "npm run package && code --install-extension zag-lang.vsix",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.9.2",
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.3.0"
  }
}
