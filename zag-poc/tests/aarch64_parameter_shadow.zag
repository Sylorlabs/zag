struct Payload { value: i32 }

fn read(payload: *Payload) i32 {
    let before: i32 = payload.*.value;
    let payload: i32 = 2;
    return before + payload;
}

fn main() i32 {
    let payload: Payload = Payload{ .value = 40 };
    return read(&payload);
}
