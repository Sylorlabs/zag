fn applyTwice(op: fn(f32) f32 ! e, x: f32) f32 {
    return op(op(x));
}

fn closureCaller(x: f32) f32 @realtime {
    return applyTwice(fn[](y: f32) f32 {
        let scratch: []f32 = zalloc(1);
        zfree(scratch);
        return y;
    }, x);
}

fn main() i32 { return 0; }
