fn cycleA(n: i32) void {
    if (n > 0) { cycleB(n - 1); }
}

fn cycleB(n: i32) void {
    if (n > 0) { cycleA(n - 1); }
    let scratch: []i32 = zalloc(1);
}

fn recursiveCaller(n: i32) void @realtime {
    cycleA(n);
}

fn main() i32 { return 0; }
