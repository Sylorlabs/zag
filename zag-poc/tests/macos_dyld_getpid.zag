// Native Mach-O dyld import smoke test.  `getpid` comes from libSystem, and
// the executable must reach it through the writer's __DATA bind slot/stub.
extern fn getpid() i64;

fn main() i32 {
    let pid: i64 = getpid();
    if (pid > 0) { return 42; }
    return 1;
}
