extern fn XOpenDisplay(name: *u8) *u8;
extern fn XCloseDisplay(display: *u8) i32;

fn main() i32 {
    let display: *u8 = XOpenDisplay(null as *u8);
    if (display == null as *u8) { return 2; }
    if (XCloseDisplay(display) != 0) { return 3; }
    return 42;
}
