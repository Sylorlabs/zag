// A non-@cabi extern must never be upgraded into an object import merely by
// selecting --emit-obj, even if the declaration is not called in this unit.
extern fn legacy_undocumented(x: i64) i64;

pub fn object_without_legacy_import(x: i64) i64 @cabi_export { return x + 1; }
