@import("std:list")

fn main() i32 {
    let words: ArrayList[i64] = make[i64](1);
    push[i64](&words, 1);
    let value: vsa_b<65> = @vsaFromWords[vsa_b<65>](words.data[0..1]);
    free[i64](&words);
    return @vsaWord(value, 0) as i32;
}
