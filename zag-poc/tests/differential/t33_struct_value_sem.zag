struct P { x: i32, y: i32 }
fn main() i32 {
    let a: P = P{ .x = 40, .y = 2 };
    let b: P = a;          // value copy
    b.x = 0;               // must not affect a
    return a.x + a.y + b.x;
}
