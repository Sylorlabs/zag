fn main() i32 {
    let g: i32 = 2;
    let inner: fn(i32) i32 = fn[g](x: i32) i32 { return x * g; };
    let outer: fn(i32) i32 = fn[inner](x: i32) i32 { return inner(x) + 2; };
    return outer(20);   // 20*2+2 = 42
}
