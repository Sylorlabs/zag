enum E { a, b, c, d }
fn cls(e: E) i32 {
    switch (e) {
        .a, .b => { return 10; }
        .c => { return 20; }
        else => { return 30; }
    }
    return 0;
}
fn main() i32 { return cls(E.a) + cls(E.b) + cls(E.c) + cls(E.d) - 28; }
