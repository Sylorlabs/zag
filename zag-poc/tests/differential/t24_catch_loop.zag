error { Odd }
fn ev(x: i32) !i32 { if (x % 2 == 1) { return error.Odd; } return x; }
fn main() i32 {
    let s: i32 = 0;
    let i: i32 = 0;
    while (i < 10) { s = s + (ev(i) catch 1); i = i + 1; }
    return s + 17;    // evens 0+2+4+6+8=20, odds 5 → 25+17=42
}
