fn main() i32 {
    let a: i32 = 5;
    let outer: fn(i32) i32 = fn[a](x: i32) i32 {
        let inner: fn(i32) i32 = fn[a](y: i32) i32 { return y * a; };
        return inner(x) + a;
    };
    return outer(7) + 2;   // 35+5+2 = 42
}
