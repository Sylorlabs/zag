fn main() i32 {
    let a: i32 = 5 | 2;                // 7
    let b: i32 = a | 8 & 12;           // & and | same prec, left assoc: (a|8)&12 = 15&12 = 12
    let o: ?i32 = 7;
    if (o) |v| { return a + b + v + 16; }   // 7+12+7+16 = 42
    return 1;
}
