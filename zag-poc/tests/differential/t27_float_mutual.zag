fn ping(x: f64, n: i32) f64 { if (n == 0) { return x; } return pong(x * 2.0, n - 1); }
fn pong(x: f64, n: i32) f64 { if (n == 0) { return x; } return ping(x + 1.0, n - 1); }
fn main() i32 { return ping(1.0, 8) as i32; }
