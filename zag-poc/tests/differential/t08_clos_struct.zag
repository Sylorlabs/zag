struct P { x: i32, y: i32 }
fn main() i32 {
    let p: P = P{ .x = 6, .y = 7 };
    let f: fn() i32 = fn[p]() i32 { return p.x * p.y; };   // aggregate capture
    p.x = 0;                                                // copy-on-close: must not affect f
    return f();
}
