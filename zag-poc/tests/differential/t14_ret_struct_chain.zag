struct P { x: i32, y: i32 }
fn mk(a: i32) P { return P{ .x = a, .y = a * 2 }; }
fn addp(a: P, b: P) P { return P{ .x = a.x + b.x, .y = a.y + b.y }; }
fn main() i32 {
    let r: P = addp(mk(3), addp(mk(4), mk(5)));   // x=12 y=24
    return r.x + r.y + 6;
}
