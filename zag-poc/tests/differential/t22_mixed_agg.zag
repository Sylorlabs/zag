struct S { name: []u8, val: ?i32, tag: i32 }
fn mk(n: []u8, v: i32) S { return S{ .name = n, .val = v, .tag = 7 }; }
fn main() i32 {
    let s: S = mk("hello", 30);
    let v: i32 = s.val orelse 0;
    return s.name.len + v + s.tag;    // 5+30+7 = 42
}
