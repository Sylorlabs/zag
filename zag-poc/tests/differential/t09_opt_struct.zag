struct P { x: i32, y: i32 }
fn find(k: i32) ?P { if (k > 0) { return P{ .x = k, .y = k + 1 }; } return null; }
fn main() i32 {
    let o: ?P = find(20);
    if (o) |p| { return p.x + p.y + 1; }
    return 1;
}
