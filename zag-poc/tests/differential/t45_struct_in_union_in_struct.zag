struct In { a: i32, b: i32 }
union U { i: In, n: i32 }
struct Out { u: U, tag: i32 }
fn main() i32 {
    let o: Out = Out{ .u = U{ .i = In{ .a = 30, .b = 5 } }, .tag = 7 };
    switch (o.u) { .i => |c| { return c.*.a + c.*.b + o.tag; } .n => |m| { return m; } }
    return 0;
}
