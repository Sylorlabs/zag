@import("std:list")

struct RawBox {
    ptr: *i8,
}

fn dispose(ptr: @consumes *i8) void {
    _zag_free(ptr);
}

fn main() i32 {
    let box: RawBox = RawBox{ .ptr = null as *i8 };
    dispose(box.ptr);
    return 0;
}
