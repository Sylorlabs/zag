@import("std:list")

@resource
struct Frame {
    bytes: @owned(free) ArrayList[u8],
}

fn frame_make() Frame {
    return Frame{ .bytes = make[u8](4) };
}

fn discard[T](value: T) void {
    return;
}

fn main() i32 {
    let frame: Frame = frame_make();
    discard[Frame](frame);
    return 0;
}
