@import("std:list")

@resource
struct Frame {
    bytes: @owned(free) ArrayList[u8],
}

fn frame_make() Frame {
    return Frame{ .bytes = make[u8](4) };
}

fn main() i32 {
    if (1 == 1) {
        let frame: Frame = frame_make();
    }
    return 0;
}
