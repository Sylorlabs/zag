@import("../selfhost/native/hot_rt_macos.zag")

fn value() i32 { return 1; }

fn main() i32 {
    while (zag_hot_should_continue()) {
        if (value() == 2) { return 42; }
        zag_hot_wait();
    }
    return 1;
}
