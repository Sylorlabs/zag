script;

// Default Script budget is 64 MiB; one byte beyond it must fail before mmap.
let builder = string_builder(67108865);
if (string_builder_failed(&builder) == 0 || string_builder_len(&builder) != 0) { return 1; }
return 0;
