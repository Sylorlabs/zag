script;

let result = process_run_timeout("printf script", 1000, 128);
if (process_result_state(result) != process_state_exited()) { return 1; }
if (!@strEq(process_result_output(result), "script")) { return 2; }
return process_result_status(result);
