script;

// Must remain a compile error until the native supervisor described in
// ZAGSCRIPT_PROCESS_BOUNDARY.md exists. Never bind this to _zag_exec_capture.
let result = process_run_timeout("printf unsafe", 10, 4096);
return result.status;
