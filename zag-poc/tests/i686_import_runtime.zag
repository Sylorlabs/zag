@import("i686_import_lib.zag")

fn main() i32 {
    let written: i32 = _zag_print("A\n");
    let value: *i32 = _zag_malloc(4);
    value.* = imported_answer() + 2;
    let result: i32 = value.*;
    let freed: i32 = _zag_free(value);
    return result;
}
