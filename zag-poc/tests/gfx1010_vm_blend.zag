fn blendDepthKernel(color: []i32, depth: []i32, value: i32, z: i32, alpha: i32) void @kernel {
    let i: i32 = @gpuBlockIdx(0) * @gpuBlockDim(0) + @gpuThreadIdx(0);
    if (z <= depth[i]) { color[i] = @gpuBlend(value, color[i], alpha); depth[i] = z; }
}

fn main() void { print_i32(0); }
