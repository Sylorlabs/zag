fn depthWriteKernel(color: []i32, depth: []i32, value: i32, z: i32) void @kernel {
    let i: i32 = @gpuBlockIdx(0) * @gpuBlockDim(0) + @gpuThreadIdx(0);
    if (z <= depth[i]) { color[i] = value; depth[i] = z; }
}

fn main() void { print_i32(0); }
