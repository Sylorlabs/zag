fn main() i32 {
    let values:*usize = _zag_malloc(8);
    if (values == null) { return 1; }
    values[0] = 19;
    values[1] = 23;
    let total:usize = values[0] + values[1];
    let freed:i32 = _zag_free(values);
    return total as i32;
}
