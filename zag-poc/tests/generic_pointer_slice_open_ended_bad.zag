@import("std:list")

fn main() i32 {
    let values: ArrayList[i64] = make[i64](2);
    push[i64](&values, 1);
    push[i64](&values, 2);
    let invalid: []i64 = values.data[1..];
    free[i64](&values);
    return invalid.len;
}
