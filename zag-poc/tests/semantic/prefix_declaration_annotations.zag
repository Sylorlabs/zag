@realtime
struct Sensor[T] {
    value: T,
}

@hot
enum SensorMode { Idle, Active }

@photonic
fn read_sensor[T](s: Sensor[T]) T {
    return s.value;
}

fn main() i32 {
    let sensor: Sensor[i32] = Sensor[i32]{ .value = 42 };
    return read_sensor[i32](sensor);
}
