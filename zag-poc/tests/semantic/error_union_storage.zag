error { E }

fn answer() !i32 { return 42; }

struct ResultBox { value: !i32 }

fn main() i32 {
    let direct: !i32 = answer();
    let boxed: ResultBox = ResultBox{ .value = answer() };
    let a: i32 = direct catch 0;
    let b: i32 = boxed.value catch 0;
    return a + b - 42;
}
