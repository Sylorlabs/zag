# Zag v2 final verification matrix

Status: **not a release verification**.  This matrix is deliberately checked
into the repository before implementation is complete so that a missing test
or runtime proof cannot be mistaken for support.  `UNSUPPORTED` entries are
release-gate failures.  `FRONTEND ONLY` means compilation artefacts are
produced, not that a target executes them.

The authoritative command is `bash tests/run_v2_release_gate.sh`.  At the
time this matrix was written it must fail: it records the unimplemented v2
surface rather than granting a premature green release.
`docs/V2_SUPPORT_MATRIX.generated.md` is separately generated from that gate's
inventory; it cannot turn an unsupported category into a release pass.

Latest authoritative post-bootstrap working-copy run (2026-07-29): **37 passing categories, 10
failing required categories, exit 1**. Passing categories include bootstrap,
v1 semantic/native compatibility, shared declared-type authority, the v2
edition/option gates, mutation-aware aggregate provenance, hard-error handling, malformed-input and deterministic
fuzz smoke, documentation/matrix checks, WASM emission regression, GPU frontend
validation, dynamic ELF ABI loading (including fail-closed shared-object
requests), and validated x86 intrinsic regressions.
Every failure below is intentional fail-closed release evidence,
not a skipped test.

| Capability | Specification | Implementation evidence | Positive evidence | Negative evidence | Runtime evidence | Status |
|---|---|---|---|---|---|---|
| Edition boundary | `V2_LANGUAGE_SPEC.md` §1 | `selfhost/v2_edition.zag`; `native/znc.zag` | `tests/run_v2_edition.sh`: v1 sources continue to compile; `check` also applies the gate | same test rejects v2 words in edition 2026 and edition 2027 words fail closed | native compiler exits nonzero and does not compile/check the rejected source | IMPLEMENTED (gate only) |
| Unimplemented v2 driver options | `V2_LANGUAGE_SPEC.md` §6 | `selfhost/v2_edition.zag`; `native/znc.zag` | `tests/run_v2_option_rejection.sh` | rejects safety, sanitizer (including compiler-style `-fsanitize` controls), unimplemented compiler-style CPU `-m`, LLVM target/feature, and Rust `-C` controls, target-feature, GPU-runtime, CLI edition, parameterized/alternate-debug, strip, and assembly/IR-output options rather than ignoring them | asserts nonzero status and no artifact | IMPLEMENTED (fail-closed gate only) |
| Hard-error artifact suppression | `V2_LANGUAGE_SPEC.md` §4 | native driver error returns | `tests/run_no_artifact_errors.sh` | same test covers arity, duplicate definition, missing main, unknown identifier, and a caller-owned output sentinel | asserts nonzero status, no fresh artifact, and no overwrite/delete of an existing output | PARTIAL / representative errors only |
| Shared typed authority | `V2_LANGUAGE_SPEC.md` §2 | `selfhost/typed.zag`; invoked by `native/znc.zag` before every target | `tests/run_typed_authority.sh` accepts named/generic types and runs the native result | rejects unknown signature/local types on every target with E0202 and representation-category return/call/assignment/operator mismatches with E0203, all without artifacts | native positive executes; rejection runs through every target driver | PARTIAL / declarations and conservative expressions; complete numeric/generic expression types and backend IR remain duplicated |
| Unsafe lexical boundary | `V2_UNSAFE_MODEL.md` | dedicated `UnsafeBlock` AST in `selfhost/ast.zag`; `selfhost/v2_edition.zag`, `parse.zag`, `typed.zag`, and `sema.zag` preserve lexical scope, unsafe functions, direct-call safety, distinct transitive effects, and verified function-value/aggregate rows | `tests/run_v2_edition.sh` executes native unsafe blocks/functions and checks formatting plus AArch64, WASM, and GPU lowering; effect adversarial suites cover indirect/generic/FFI propagation | rejects safe calls to unsafe functions, unsafe function-value conversion, safe-scope raw dereference, const-pointer mutation, ordinary type errors inside unsafe, and `Unsafe` reaching constrained functions, without artifacts | native x86-64 execution; AArch64/WASM/GPU compile evidence only | PARTIAL / source-span audit records and the complete unsafe-operation inventory remain |
| Raw pointers, provenance, alignment, aliasing | `V2_MEMORY_MODEL.md` | typed qualifier recognition, named-origin ownership/borrow flow with scalar auxiliary parameters, bounded mutation-aware field-path provenance, scalar captured-callback ownership with `close`, root-only zero-initialized one-word primitive-scalar and raw-pointer globals, raw-pointer ordering rejection, pre-lowering machine-control checks in `selfhost/typed.zag`, and `--safety=checked` native x86-64 null/alignment, overflow-safe raw-index formation, tracked ordinary/raw-slice bounds and freed-region guards, plus process-lifetime small-block quarantine and fail-closed ordinary address-reuse rejection | `tests/run_v2_edition.sh` executes `*const` construction/dereference, checked valid 24-byte aggregate and tracked in-bounds heap access, checked small-allocation ABA quarantine with a live successor, rejection of a kernel-reissued dedicated large address, release-mode reuse control, owned-return/release positives, a returned scalar captured callback followed by `close`, scalar and raw-pointer global read/write/release, checked raw-slice release, caller-provided aggregate pointer returns, and a borrow with a scalar auxiliary parameter; it rejects raw-pointer ordering, a pointer auxiliary lifetime, a released raw-slice use, a freed pointer's attempted integer/global-store escape, an `Allocation` handle callback capture, and an `Allocation` handle global declaration before lowering. `tests/run_v2_aggregate_provenance.sh` covers safe field/copy clearing and fail-closed paths beyond the cap | edition 2026 qualifier rejection, nullable dereference, checked null/misaligned dereference, tracked one-past heap access, overflowing raw-pointer index loads/stores, cross-address-space casts, raw-pointer ordering, safe-scope dereference, const mutation, tested double/use-after-free and ownership/borrow violations, callee-frame address returns, pointer/aggregate callback capture, aggregate/nonzero-initialized globals, callback use after close, and named aggregate pass/return/store escapes reject without artifacts | native x86-64 execution; `run_allocator_lifetime.sh` additionally proves stale dereference traps after a freed dedicated large mapping is unmapped | PARTIAL / checked ordinary allocator ABA is fail-closed within a bounded cumulative lifetime table, and scalar callback reclamation plus narrow scalar/pointer BSS globals exist, but raw pointers do not carry identity, arbitrary heap graphs, untracked/custom allocators, aggregate global contracts, pointer/aggregate callback capture, and general reference lifetimes remain |
| Checked and unchecked slices | `V2_MEMORY_MODEL.md` | v1 native slice lowering | `tests/run_native.sh` covers v1 checked paths | no v2 unchecked boundary test | v1 native execution only | PARTIAL / not v2 support |
| Integer overflow and conversions | `V2_LANGUAGE_SPEC.md` §5 | distributed v1 numeric lowering | selected v1 numeric tests | no v2 checked/wrapping/saturating contract suite | v1 native execution only | PARTIAL / not v2 support |
| Allocation, resize, free, custom allocators | `V2_MEMORY_MODEL.md`, `V2_ALLOCATOR_GUIDE.md` | `_zag_malloc`, `_zag_realloc`, `_zag_free` lowering with bounded large-map provenance and allocator telemetry; checked native `std/allocator.zag` provides fallible opaque `SystemAllocator.allocate`, `allocate_zeroed`, `resize`, consuming `deallocate`, and checked byte operations. The compiler owns the native pointer/capacity/alignment/generation/identity layout. `Allocation` aggregate insertion is an affine move, aggregate copies fail closed, and field deallocation, resize, or a proven non-extern `@consumes @returns_owner` helper retires the shared old capability identity and binds one replacement. Bounded retained fixed-buffer and arena slices permit named construction, block allocation, checked byte access with backing-handle revalidation, generation-invalidating reset, a proven ephemeral allocate/reset loop, and top-level `deinit` | `tests/run_allocator_lifetime.sh` executes allocation, reuse, resize, large-map accounting, and Script allocation witnesses; `tests/run_v2_edition.sh` executes opaque allocation access, aggregate-field resize/replacement use, exact helper transfer from an aggregate field, plus fixed-buffer allocation/write/read/reset/deinit and rejects literals, descriptor-field access, casts, direct copies, aggregate copies, use of a moved aggregate source binding, field-consume/original-alias reuse, stale post-resize/helper aggregate paths, fresh-owner transfer annotations, stale blocks, and stale backing; `tests/run_fixed_buffer_runtime.sh` proves 1,100 public reset cycles reuse bounded block rows; `tests/run_arena_allocator.sh` executes bounded arena reset/deinit and rejects aliases and stale blocks | rejects invalid/free/stale SystemAllocator handles, source descriptor construction/access/casts, affine capability copies, stale aliases, invalid transfer summaries, retained block aliases, stale backing/blocks, escapes, and unproven control-flow lifetimes | x86-64 native execution for SystemAllocator and checked retained-region runtime metadata | PARTIAL / native handles are bounded to 8-byte alignment; custom/debug allocators and a general dynamic lifetime system remain unsupported |
| Layout and ABI representations | `V2_MEMORY_MODEL.md`, `V2_ABI.md` | native size/layout code | existing native aggregate tests | no C layout differential test | no bidirectional C execution | UNSUPPORTED |
| Volatile and MMIO | `V2_MEMORY_MODEL.md` | edition-2027 native `@volatileLoad`/`@volatileStore` on explicit 64-bit raw pointers plus exact-width `@volatileLoad8`/`@volatileLoad16`/`@volatileLoad32` and store companions on `u8`/`u16`/`u32` pointers; checked-mode raw-access probe and bounded byte `MmioRegion` helper | `tests/run_v2_edition.sh` executes the checked word and byte transactions (including value-call register preservation); `tests/run_x86_volatile_widths.sh` executes halfword/dword store-loads, inspects exact x86 encodings, and covers width/mutability/unsafe negatives; `tests/run_mmio_region.sh` executes bounded byte access and rejects an out-of-range offset | unsafe/effect/type/const-store/width negatives | native x86-64 checked execution | PARTIAL / fixed 8/16/32/64-bit transactions plus a source-level byte-range helper; no physical device validation, opaque hardware authority, or atomic ordering |
| Atomics, fences, memory orders | `V2_CONCURRENCY_MODEL.md`, `V2_CONCURRENCY_GUIDE.md` | edition-2027 compiler-reserved `AtomicI64` is opaque, receiver-only storage lowered to native x86-64 load/store/RMW/CAS instructions. Legacy raw i64 atomic intrinsics, literal-validated order forms, and `@atomicFence(order)` remain unsafe and bounded; Linux x86-64 also exposes raw `@atomicWait32`/`@atomicWake32` futex calls. Relaxed fences are no-ops and non-relaxed fences emit a conservative full hardware fence | `tests/run_v2_atomic_storage.sh` executes receiver operations, inspects locked lowering, and rejects storage read/copy/address/aggregate/parameter escapes; `tests/run_v2_atomic_exchange.sh`, `tests/run_v2_atomic_orders.sh`, and `tests/run_x86_atomic_futex.sh` cover legacy operations, literal-order validation, and futex evidence | safe-scope, storage escape, non-`i64` values, non-`i32` futex values, wrong arity, invalid order combinations, null, and misaligned-address paths reject/trap | native x86-64 single-thread execution only | PARTIAL / compiler-reserved unsafe AtomicI64 plus fixed raw i64 and futex slices; no language-wide fence semantics, litmus suite, or race proof |
| Threads and blocking semantics | `V2_CONCURRENCY_MODEL.md`, `V2_CONCURRENCY_GUIDE.md` | compiler-lowered Linux/x86-64 unsafe `@threadSpawn(direct captureless fn() void) *opaque` plus direct `fn(i64) void` with one copied scalar argument, and consuming `@threadJoin(*opaque)`; separate guarded child stack, persistent clear-TID cell, child direct-call/`SYS_exit`, and futex join | `tests/run_x86_thread_spawn.sh` runs four workers through a futex gate, observes `clone` and `FUTEX_WAIT` under `strace -f`, validates joined atomic work and a copied scalar worker argument, and rejects safe-scope, worker/argument/handle-shape, pure, and leak cases | pointer/aggregate worker arguments, returns, closures, generic/extern workers, detach, duplicate/embedded joins, mutexes, condition variables, TLS, and non-Linux-x86-64 outputs remain rejected or unsupported | native Linux/x86-64 only | PARTIAL / a join-only kernel-thread boundary with one copied scalar argument exists; no general lifetime, scheduler, memory-model, or race-freedom claim |
| Effect propagation | `V2_EFFECT_MODEL.md` | `selfhost/sema.zag` tracks distinct `Atomic`, `FFI`, `Device`, and `GPUHost` effects, preserves all ten bits through function-value assignment, enforces aggregate rows at construction/mutation/call, resolves concrete methods, instantiates generic callback effects, and gives unknown indirect calls the complete universe | `tests/run_semantics.sh`; `tests/run_v2_edition.sh`; `tests/run_v2_effect_adversarial.sh`; `tests/run_v2_kernel_effects.sh` cover direct, callback, reassignment, aggregate, method, generic, FFI, atomic, Device, GPUHost, and opaque paths | violations reject without an artifact; unverified interface/computed dispatch fails closed | semantic check and GPU frontend boundary | COMPLETE for the implemented effect universe; new operations require explicit classification before support |
| C ABI, static objects, dynamic libraries | `V2_ABI.md` | v1 `extern` parsing/native call lowering plus native x86-64 ELF64 PT_INTERP/DT_NEEDED/GOT/RELATIVE dynamic writer; bounded direct captureless scalar/pointer callbacks lower as raw code pointers; edition-2027 dynamic `@cabi` imports support scalar/pointer values plus up to four `f64` values in `XMM0`–`XMM3`; scalar/pointer `pub fn @cabi_export` objects support self-contained output and direct `@cabi` call imports as `R_X86_64_PLT32` relocations | `tests/run_dynamic_abi.sh` proves a Vulkan loader call, scalar, one/two/mixed-register `f64` v2 `@cabi` imports, and a libc `qsort` callback; `tests/run_x86_64_cabi_object.sh` inspects the `ET_REL`, global/undefined `FUNC`, no-relocation and PLT32 contracts and links C callers/implementations that execute both directions | rejects/omits `f32`, aggregate, variadic, generic, float-callback, archive, shared output, data/TLS/GOT relocation, broad ownership, unwind, and visibility policy | native x86-64 dynamic execution and scalar C-to-Zag/Zag-to-C object harnesses | PARTIAL / bounded scalar/`f64` dynamic imports, one direct callback form, self-contained scalar exports, and direct-call object relocations exist; the specified v2 ABI remains incomplete |
| CPU intrinsics, SIMD, inline assembly | `V2_LANGUAGE_SPEC.md`, `V2_UNSAFE_MODEL.md` | native x86-64 baseline `byte_swap64`, `leading_zeros`, `@prefetch`, POPCNT, BMI1 ANDN, trailing-zero lowering, and bounded unsafe i32x4 add/subtract/and/or/xor SSE2 lowering | scalar test scripts plus `tests/run_x86_simd_add_i32x4.sh`, `tests/run_x86_simd_sub_i32x4.sh`, and `tests/run_x86_simd_bitwise_i32x4.sh` inspect machine bytes and execute bounded results | vector types/ABI, broader SIMD operations, inline-assembly constraints/clobbers, and full target-feature declaration/effect checking remain rejected | native x86-64 execution for validated scalar/cache-hint intrinsics and five four-lane SSE2 operations | PARTIAL / no v2 operand/clobber checked asm or portable SIMD contract |
| GPU address spaces and kernel effects | `V2_GPU_MODEL.md`, `V2_EFFECT_MODEL.md` | `@kernel` rejects all implemented host effects but admits the distinct `Device` effect through direct helpers; host resource/launch operations carry `GPUHost`; MLIR/gfx bundle emitters still lack a complete v2 typed GPU authority | `tests/run_native_gpu.sh` checks emitted output; `tests/run_v2_kernel_effects.sh` proves frontend emission, direct Device-helper acceptance, and rejection of I/O, raw atomic, C-ABI, and host-GPU paths | host effects reject before emission; opaque indirect effects remain fail-closed | semantic/frontend only | PARTIAL / no physical GPU runtime or complete device type/effect model |
| GPU target binary and physical dispatch | `V2_GPU_MODEL.md` | none: no device enumeration/context/buffer/dispatch/readback path | none | no OOB/effect rejection at runtime | none | UNSUPPORTED |
| Sanitizers and debug allocator | `V2_SAFETY_TOOLING.md` | native x86-64 `--sanitize=memory` enables bounded ordinary-allocation checked provenance and address quarantine, records exact requested length to trap small-block class-slack overflows, reserves and validates a 16-byte trailing red zone on small allocations, poisons quarantined freed payload bytes with `0xA5`, and exits nonzero on a nonzero live-allocation witness; dedicated ordinary allocations above 512 KiB add a trailing 4 KiB `PROT_NONE` guard page; checked `SystemAllocator` handles retain generation and allocator-identity validation | `tests/run_v2_edition.sh` executes released-allocation, out-of-band libc red-zone corruption, guarded large-allocation, logical-overflow, out-of-band quarantined-payload poison observation, and stale-generation/identity handle witnesses | unsupported target rejects without artifact; ordinary direct leaks are also rejected statically | native x86-64 execution | PARTIAL / no leading red zones, allocation-site reports, stack/global instrumentation, or custom allocator tracking; the large-allocation guard is a coarse trailing page boundary, and untracked allocator families remain outside quarantine |
| Fuzzing and malformed-input corpus | `V2_IMPLEMENTATION_PLAN.md` | `tests/run_crash_corpus.sh`, `tests/run_fuzz_smoke.sh`; `tests/crash_corpus/` | crash corpus rejects ten minimized malformed sources; deterministic byte smoke has seven cases | both suites assert nonzero exit, timeout failure, no artifact, and no signal termination | compiler invocation only | PARTIAL / no coverage-guided fuzzing or sanitizers |
| Differential and performance evidence | `V2_IMPLEMENTATION_PLAN.md` | legacy differential scripts exist | `tests/run_differential.sh` is v1 evidence | no v2 semantic differential suite | no v2 benchmarks | PARTIAL / not v2 support |

## Verified v1 regression evidence, not v2 completion

The following commands remain useful regression evidence for the frozen v1
compiler.  They do not discharge a v2 row above: `./bootstrap.sh`,
`bash tests/run_semantics.sh`, `bash tests/run_native.sh`,
`bash tests/run_native_authority.sh`, `bash tests/run_native_wasm.sh`, and
`bash tests/run_native_gpu.sh`.  The GPU command validates emitted MLIR and a
restricted bundle only; it must never be cited as a physical GPU execution
test. The WASM command validates pure-Zag emission and structure only; runtime
execution remains unsupported until its runtime is implemented in Zag.

## Release decision

Do not release Zag v2.  A release requires every row marked `UNSUPPORTED`,
`PARTIAL`, or `FRONTEND ONLY` to gain a specification-conformant implementation
plus positive and negative tests, and runtime validation where the row calls
for execution.  The release gate intentionally returns nonzero until then.
