# Zag v2 final verification matrix

Status: **not a release verification**.  This matrix is deliberately checked
into the repository before implementation is complete so that a missing test
or runtime proof cannot be mistaken for support.  `UNSUPPORTED` entries are
release-gate failures.  `FRONTEND ONLY` means compilation artefacts are
produced, not that a target executes them.

The authoritative command is `bash tests/run_v2_release_gate.sh`.  At the
time this matrix was written it must fail: it records the unimplemented v2
surface rather than granting a premature green release.
`docs/V2_SUPPORT_MATRIX.generated.md` is separately generated from that gate's
inventory; it cannot turn an unsupported category into a release pass.

Latest isolated working-copy run (2026-07-26): **24 passing categories, 10
failing required categories, exit 1**. Passing categories include bootstrap,
v1 semantic/native compatibility, shared declared-type authority, the v2
edition/option gates, mutation-aware aggregate provenance, hard-error handling, malformed-input and deterministic
fuzz smoke, documentation/matrix checks, WASM emission regression, GPU frontend
validation, dynamic ELF ABI loading (including fail-closed shared-object
requests), and validated x86 intrinsic regressions.
Every failure below is intentional fail-closed release evidence,
not a skipped test.

| Capability | Specification | Implementation evidence | Positive evidence | Negative evidence | Runtime evidence | Status |
|---|---|---|---|---|---|---|
| Edition boundary | `V2_LANGUAGE_SPEC.md` §1 | `selfhost/v2_edition.zag`; `native/znc.zag` | `tests/run_v2_edition.sh`: v1 sources continue to compile; `check` also applies the gate | same test rejects v2 words in edition 2026 and edition 2027 words fail closed | native compiler exits nonzero and does not compile/check the rejected source | IMPLEMENTED (gate only) |
| Unimplemented v2 driver options | `V2_LANGUAGE_SPEC.md` §6 | `selfhost/v2_edition.zag`; `native/znc.zag` | `tests/run_v2_option_rejection.sh` | rejects safety, sanitizer (including compiler-style `-fsanitize` controls), unimplemented compiler-style CPU `-m`, LLVM target/feature, and Rust `-C` controls, target-feature, GPU-runtime, CLI edition, parameterized/alternate-debug, strip, and assembly/IR-output options rather than ignoring them | asserts nonzero status and no artifact | IMPLEMENTED (fail-closed gate only) |
| Hard-error artifact suppression | `V2_LANGUAGE_SPEC.md` §4 | native driver error returns | `tests/run_no_artifact_errors.sh` | same test covers arity, duplicate definition, missing main, unknown identifier, and a caller-owned output sentinel | asserts nonzero status, no fresh artifact, and no overwrite/delete of an existing output | PARTIAL / representative errors only |
| Shared typed authority | `V2_LANGUAGE_SPEC.md` §2 | `selfhost/typed.zag`; invoked by `native/znc.zag` before every target | `tests/run_typed_authority.sh` accepts named/generic types and runs the native result | rejects unknown signature/local types on every target with E0202 and representation-category return/call/assignment/operator mismatches with E0203, all without artifacts | native positive executes; rejection runs through every target driver | PARTIAL / declarations and conservative expressions; complete numeric/generic expression types and backend IR remain duplicated |
| Unsafe lexical boundary | `V2_UNSAFE_MODEL.md` | dedicated `UnsafeBlock` AST in `selfhost/ast.zag`; `selfhost/v2_edition.zag`, `parse.zag`, `typed.zag`, and `sema.zag` preserve lexical scope, unsafe functions, direct-call safety, and the `Unsafe` effect | `tests/run_v2_edition.sh` executes native unsafe blocks/functions and checks formatting plus AArch64, WASM, and GPU lowering | rejects safe calls to unsafe functions, unsafe function-value conversion, safe-scope raw dereference, const-pointer mutation, ordinary type errors inside unsafe, and `Unsafe` reaching `@pure`, without artifacts | native x86-64 execution; AArch64/WASM/GPU compile evidence only | PARTIAL / direct unsafe blocks and functions implemented; source-span audit records, typed indirect-call contracts/generics/FFI propagation, and the complete unsafe-operation inventory remain |
| Raw pointers, provenance, alignment, aliasing | `V2_MEMORY_MODEL.md` | typed qualifier recognition, named-origin ownership/borrow flow with scalar auxiliary parameters, bounded mutation-aware field-path provenance, scalar captured-callback ownership with `close`, root-only zero-initialized one-word primitive-scalar and raw-pointer globals, raw-pointer ordering rejection, pre-lowering machine-control checks in `selfhost/typed.zag`, and `--safety=checked` native x86-64 null/alignment plus tracked ordinary and raw-slice allocation bounds/freed-region guards | `tests/run_v2_edition.sh` executes `*const` construction/dereference, checked valid 24-byte aggregate and tracked in-bounds heap access, owned-return/release positives, a returned scalar captured callback followed by `close`, scalar and raw-pointer global read/write/release, checked raw-slice release, caller-provided aggregate pointer returns, and a borrow with a scalar auxiliary parameter; it rejects raw-pointer ordering, a pointer auxiliary lifetime, a released raw-slice use, a freed pointer's attempted integer/global-store escape, an `Allocation` handle callback capture, and an `Allocation` handle global declaration before lowering. `tests/run_v2_aggregate_provenance.sh` covers safe field/copy clearing and fail-closed paths beyond the cap | edition 2026 qualifier rejection, nullable dereference, checked null/misaligned dereference and tracked one-past heap access, cross-address-space casts, raw-pointer ordering, safe-scope dereference, const mutation, tested double/use-after-free and ownership/borrow violations, callee-frame address returns, pointer/aggregate callback capture, aggregate/nonzero-initialized globals, callback use after close, and named aggregate pass/return/store escapes reject without artifacts | native x86-64 execution; `run_allocator_lifetime.sh` additionally proves stale dereference traps after a freed dedicated large mapping is unmapped | PARTIAL / checked mode has bounded ordinary/raw-slice runtime metadata, scalar callback reclamation, narrow scalar/pointer BSS globals, and ABA-resistant SystemAllocator handles, but arbitrary heap graphs, custom allocator tracking, aggregate global contracts, pointer/aggregate callback capture, and general runtime use-after-free instrumentation remain |
| Checked and unchecked slices | `V2_MEMORY_MODEL.md` | v1 native slice lowering | `tests/run_native.sh` covers v1 checked paths | no v2 unchecked boundary test | v1 native execution only | PARTIAL / not v2 support |
| Integer overflow and conversions | `V2_LANGUAGE_SPEC.md` §5 | distributed v1 numeric lowering | selected v1 numeric tests | no v2 checked/wrapping/saturating contract suite | v1 native execution only | PARTIAL / not v2 support |
| Allocation, resize, free, custom allocators | `V2_MEMORY_MODEL.md`, `V2_ALLOCATOR_GUIDE.md` | `_zag_malloc`, `_zag_realloc`, `_zag_free` lowering with bounded large-map provenance and allocator telemetry; checked native `std/allocator.zag` provides fallible `SystemAllocator.allocate`, `allocate_zeroed`, `resize`, and consuming `deallocate` over `{ptr, exact_capacity, alignment, generation, allocator_id}` handles, with receiver/handle identity binding. A fixed-buffer retained-owner handoff admits only construction from one named backing and top-level `deinit` into a fresh binding | `tests/run_allocator_lifetime.sh` executes allocation, reuse, resize, large-map accounting, and Script allocation witnesses; `tests/run_v2_edition.sh` executes fixed-buffer construction/deinit; `tests/run_fixed_buffer_runtime.sh` proves 1,100 reset cycles reuse bounded block rows and rejects an allocation after backing release | rejects invalid/free/stale SystemAllocator handles and fixed-buffer block allocation, aliases, and stale backing use | x86-64 native execution for SystemAllocator and checked fixed-buffer runtime metadata | PARTIAL / native handles are bounded to 8-byte alignment; fixed-buffer allocation/reset, custom/arena allocators, and a general dynamic lifetime system remain unsupported |
| Layout and ABI representations | `V2_MEMORY_MODEL.md`, `V2_ABI.md` | native size/layout code | existing native aggregate tests | no C layout differential test | no bidirectional C execution | UNSUPPORTED |
| Volatile and MMIO | `V2_MEMORY_MODEL.md` | edition-2027 native `@volatileLoad`/`@volatileStore` on explicit 64-bit raw pointers plus exact-width `@volatileLoad8`/`@volatileLoad16`/`@volatileLoad32` and store companions on `u8`/`u16`/`u32` pointers; checked-mode raw-access probe | `tests/run_v2_edition.sh` executes the checked word and byte transactions (including value-call register preservation); `tests/run_x86_volatile_widths.sh` executes halfword/dword store-loads, inspects exact x86 encodings, and covers width/mutability/unsafe negatives | unsafe/effect/type/const-store/width negatives | native x86-64 checked execution | PARTIAL / fixed 8/16/32/64-bit transactions only; no physical device validation, address-capability contract, or atomic ordering |
| Atomics, fences, memory orders | `V2_CONCURRENCY_MODEL.md`, `V2_CONCURRENCY_GUIDE.md` | edition-2027 native `@atomicLoad64`, `@atomicStore64`, `@atomicExchange64`, `@atomicCompareExchange64`, `@atomicFetchAdd64`, `@atomicFetchSub64`, `@atomicFetchAnd64`, `@atomicFetchOr64`, and `@atomicFetchXor64` lower to locked x86-64 memory transactions; literal-validated load/store/RMW/CAS order suffixes provide the bounded order subset, with separate fail-closed CAS success/failure validation; bitwise fetches use a lock-free, non-wait-free compare-exchange retry loop; legacy `@memoryFence` emits `mfence` but carries `Unsafe` without a typed ordering model; null/alignment checks are unconditional and checked mode also probes tracked allocation liveness/bounds | `tests/run_v2_atomic_exchange.sh` executes all nine fixed operations and inspects the emitted ELF; `tests/run_v2_atomic_orders.sh` executes valid load/store/RMW/CAS order forms, rejects invalid/dynamic/CAS-failure orders, and inspects MOV versus locked lowering; `tests/run_v2_edition.sh` rejects a pure fence | safe-scope, non-mutable pointer, non-`i64` bound values, wrong arity, invalid order combinations, null, and misaligned-address paths reject/trap | native x86-64 single-thread execution only | PARTIAL / fixed unsafe i64 operations plus bounded literal validation for load/store/RMW/CAS; no atomic storage type, typed fence order, language fence semantics, litmus suite, or race proof |
| Threads and blocking semantics | `V2_CONCURRENCY_MODEL.md` | `native/thread_rt.zag` is runtime-oriented only | none for public v2 syntax | no realtime/effect rejection suite | no v2 spawn/join stress suite | UNSUPPORTED |
| Effect propagation | `V2_EFFECT_MODEL.md` | `selfhost/sema.zag` infers `Unsafe` from unsafe blocks/direct calls, refreshes latent effects when a function-valued local is reassigned, and gives aggregate/index/computed indirect calls the complete implemented effect universe until their rows are modeled | `tests/run_semantics.sh`; `tests/run_v2_edition.sh`; `tests/run_v2_effect_adversarial.sh` reject direct, callback, reassigned-local, and opaque aggregate-call paths in `@pure`/`@realtime` contracts | rejects the effect contract without an artifact; opaque indirect calls fail closed rather than inheriting a field spelling | semantic check only | PARTIAL / direct and bounded function-local propagation plus fail-closed aggregate indirection; generic/verified aggregate rows/FFI/GPU/runtime-intrinsic effect coverage remains |
| C ABI, static objects, dynamic libraries | `V2_ABI.md` | v1 `extern` parsing/native call lowering plus native x86-64 ELF64 PT_INTERP/DT_NEEDED/GOT/RELATIVE dynamic writer; bounded direct captureless scalar/pointer callbacks lower as raw code pointers; edition-2027 self-contained scalar/pointer `pub fn @cabi_export` can emit a relocation-free x86-64 `ET_REL` object | `tests/run_dynamic_abi.sh` proves a Vulkan loader call, v2 `@cabi` imports and a libc `qsort` callback; `tests/run_x86_64_cabi_object.sh` inspects the `ET_REL`/global `FUNC` symbol/no-relocation contract and links a C caller which executes a Zag export | rejects/omits floats, aggregates, variadics, generics, callbacks, archives, shared output, foreign imports/data/relocations in object mode, broad ownership, unwind, and visibility policy | native x86-64 dynamic execution and one scalar C-to-Zag object harness | PARTIAL / bounded scalar imports, one direct callback form, and one relocation-free scalar export form exist; the specified v2 ABI remains incomplete |
| CPU intrinsics, SIMD, inline assembly | `V2_LANGUAGE_SPEC.md`, `V2_UNSAFE_MODEL.md` | native x86-64 baseline `byte_swap64`, `leading_zeros`, `@prefetch`, POPCNT, BMI1 ANDN, trailing-zero lowering, and bounded unsafe `@simdAddI32x4` SSE2 lowering | `tests/run_x86_byte_swap.sh`, `tests/run_x86_leading_zeros.sh`, `tests/run_x86_prefetch.sh`, `tests/run_x86_popcount.sh`, `tests/run_x86_andn.sh`, `tests/run_x86_trailing_zeros.sh`, and `tests/run_x86_simd_add_i32x4.sh` inspect machine bytes and execute bounded results | vector types/ABI, additional SIMD operations, inline-assembly constraints/clobbers, and full target-feature declaration/effect checking remain rejected | native x86-64 execution for validated scalar/cache-hint intrinsics and a four-lane SSE2 add | PARTIAL / no v2 operand/clobber checked asm or portable SIMD contract |
| GPU address spaces and kernel effects | `V2_GPU_MODEL.md`, `V2_EFFECT_MODEL.md` | `@kernel` rejects all implemented host effects before MLIR/gfx output; MLIR/gfx bundle emitters still lack a complete v2 typed GPU authority | `tests/run_native_gpu.sh` checks emitted output; `tests/run_v2_kernel_effects.sh` proves an effect-free kernel emits and rejects direct I/O, raw atomic, and C-ABI paths | no address-space/device-helper/structured-GPU-effect rejection suite | semantic/frontend only | PARTIAL / no physical GPU runtime or complete device type/effect model |
| GPU target binary and physical dispatch | `V2_GPU_MODEL.md` | none: no device enumeration/context/buffer/dispatch/readback path | none | no OOB/effect rejection at runtime | none | UNSUPPORTED |
| Sanitizers and debug allocator | `V2_SAFETY_TOOLING.md` | native x86-64 `--sanitize=memory` enables bounded ordinary-allocation checked provenance, poisons freed payload bytes with `0xA5`, and exits nonzero on a nonzero live-allocation witness; checked `SystemAllocator` handles retain generation and allocator-identity validation | `tests/run_v2_edition.sh` executes released-allocation, poison-reuse, and stale-generation/identity handle witnesses | unsupported target rejects without artifact; ordinary direct leaks are also rejected statically | native x86-64 execution | PARTIAL / no red zones, guard pages, allocation-site reports, or custom allocator tracking; raw-pointer ABA remains outside the handle boundary |
| Fuzzing and malformed-input corpus | `V2_IMPLEMENTATION_PLAN.md` | `tests/run_crash_corpus.sh`, `tests/run_fuzz_smoke.sh`; `tests/crash_corpus/` | crash corpus rejects ten minimized malformed sources; deterministic byte smoke has seven cases | both suites assert nonzero exit, timeout failure, no artifact, and no signal termination | compiler invocation only | PARTIAL / no coverage-guided fuzzing or sanitizers |
| Differential and performance evidence | `V2_IMPLEMENTATION_PLAN.md` | legacy differential scripts exist | `tests/run_differential.sh` is v1 evidence | no v2 semantic differential suite | no v2 benchmarks | PARTIAL / not v2 support |

## Verified v1 regression evidence, not v2 completion

The following commands remain useful regression evidence for the frozen v1
compiler.  They do not discharge a v2 row above: `./bootstrap.sh`,
`bash tests/run_semantics.sh`, `bash tests/run_native.sh`,
`bash tests/run_native_authority.sh`, `bash tests/run_native_wasm.sh`, and
`bash tests/run_native_gpu.sh`.  The GPU command validates emitted MLIR and a
restricted bundle only; it must never be cited as a physical GPU execution
test. The WASM command validates pure-Zag emission and structure only; runtime
execution remains unsupported until its runtime is implemented in Zag.

## Release decision

Do not release Zag v2.  A release requires every row marked `UNSUPPORTED`,
`PARTIAL`, or `FRONTEND ONLY` to gain a specification-conformant implementation
plus positive and negative tests, and runtime validation where the row calls
for execution.  The release gate intentionally returns nonzero until then.
