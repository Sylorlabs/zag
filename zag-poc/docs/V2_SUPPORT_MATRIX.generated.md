# Generated v2 release-gate support matrix
Generated by `bash tests/generate_v2_support_matrix.sh`; do not edit manually.

| Category | Gate status | Evidence source |
|---|---|---|
| bootstrap/rebuild | Required test gate: PASS only when its command exits 0 |
| v1 semantic compatibility | Required test gate: PASS only when its command exits 0 |
| v1 native execution | Required test gate: PASS only when its command exits 0 |
| shared declared-type authority | Required test gate: PASS only when its command exits 0 |
| v2 edition boundary | Required test gate: PASS only when its command exits 0 |
| unsafe lexical and raw-pointer boundary | Required test gate: PASS only when its command exits 0 |
| try-wrapped borrow identity | Required test gate: PASS only when its command exits 0 |
| mutation-aware aggregate provenance | Required test gate: PASS only when its command exits 0 |
| fixed-buffer checked runtime authority | Required test gate: PASS only when its command exits 0 |
| retained arena allocator authority | Required test gate: PASS only when its command exits 0 |
| v2 atomic i64 operations | Required test gate: PASS only when its command exits 0 |
| v2 atomic load/store memory-order validation | Required test gate: PASS only when its command exits 0 |
| fail-closed indirect effect boundary | Required test gate: PASS only when its command exits 0 |
| fail-closed kernel host-effect boundary | Required test gate: PASS only when its command exits 0 |
| v2 option rejection | Required test gate: PASS only when its command exits 0 |
| malformed-input crash corpus | Required test gate: PASS only when its command exits 0 |
| deterministic fuzz smoke | Required test gate: PASS only when its command exits 0 |
| hard errors leave no artifact | Required test gate: PASS only when its command exits 0 |
| documentation consistency | Required test gate: PASS only when its command exits 0 |
| generated support matrix | Required test gate: PASS only when its command exits 0 |
| WASM emission regression (no runtime) | Required test gate: PASS only when its command exits 0 |
| GPU frontend validation (not execution) | Required test gate: PASS only when its command exits 0 |
| dynamic ELF ABI boundary | Required test gate: PASS only when its command exits 0 |
| x86-64 C ABI object export boundary | Required test gate: PASS only when its command exits 0 |
| validated x86 POPCNT intrinsic | Required test gate: PASS only when its command exits 0 |
| validated x86 BMI1 ANDN intrinsic | Required test gate: PASS only when its command exits 0 |
| validated x86 trailing-zeros intrinsic | Required test gate: PASS only when its command exits 0 |
| validated x86 byte-swap intrinsic | Required test gate: PASS only when its command exits 0 |
| validated x86 leading-zeros intrinsic | Required test gate: PASS only when its command exits 0 |
| validated x86 prefetch intrinsic | Required test gate: PASS only when its command exits 0 |
| validated x86 volatile MMIO widths | Required test gate: PASS only when its command exits 0 |
| bounded MMIO-region authority | Required test gate: PASS only when its command exits 0 |
| validated x86 SSE2 SIMD add | Required test gate: PASS only when its command exits 0 |
| pointer and memory model | REQUIRED FAILURE: UNSUPPORTED — raw pointer categories and lexical checks exist, but provenance/alignment/lifetime instrumentation is incomplete |
| allocator and reclamation | REQUIRED FAILURE: UNSUPPORTED — checked native SystemAllocator handles and bounded retained fixed-buffer/arena regions exist, but opaque language capabilities, custom/debug allocators, and a general lifetime model are incomplete |
| volatile/MMIO | REQUIRED FAILURE: UNSUPPORTED — checked native 8/16/32-bit and word transactions plus bounded byte MmioRegion access exist, but physical device validation, opaque hardware authority, and the complete MMIO contract are incomplete |
| atomics and concurrency | REQUIRED FAILURE: UNSUPPORTED — fixed unsafe x86-64 i64 operations plus literal-validated load/store/RMW/CAS/fence orders exist, but atomic storage, language-wide fence semantics, threads, litmus evidence, and a v2 concurrency model remain incomplete |
| C ABI and dynamic linking | REQUIRED FAILURE: UNSUPPORTED — checked v2 @cabi scalar dynamic imports, a direct captureless scalar/pointer callback, and a relocation-free self-contained scalar @cabi_export ET_REL object exist, but general bidirectional ABI, relocation/static archive/shared-object conformance, and unload/lifetime contracts are incomplete |
| CPU intrinsics/SIMD/inline assembly | REQUIRED FAILURE: UNSUPPORTED — validated x86 scalar intrinsics and bounded unsafe SSE2 i32x4 add exist, but vector types/ABI, additional SIMD, inline-assembly constraints/clobbers, and full target-feature/effect checking are incomplete |
| effect adversarial suite | REQUIRED FAILURE: UNSUPPORTED — opaque aggregate/computed indirect calls and host effects in @kernel now fail closed, but verified aggregate rows, device-helper propagation, and distinct Atomic/FFI/GPU effect models remain incomplete |
| physical GPU execution | REQUIRED FAILURE: UNSUPPORTED — no runtime enumerate/allocate/dispatch/readback path |
| sanitizers | REQUIRED FAILURE: UNSUPPORTED — bounded native --sanitize=memory has exact requested-length bounds and deterministic free poisoning, but red zones, guard pages, allocation-site reports, and custom allocator coverage are incomplete |
| documentation verification map | REQUIRED FAILURE: UNSUPPORTED — verification matrix exists but records incomplete required capabilities |
