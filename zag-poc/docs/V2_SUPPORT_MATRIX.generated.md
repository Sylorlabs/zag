# Generated v2 release-gate support matrix
Generated by `bash tests/generate_v2_support_matrix.sh`; do not edit manually.

| Category | Gate status | Evidence source |
|---|---|---|
| bootstrap/rebuild | Required test gate: PASS only when its command exits 0 |
| v1 semantic compatibility | Required test gate: PASS only when its command exits 0 |
| v1 native execution | Required test gate: PASS only when its command exits 0 |
| shared declared-type authority | Required test gate: PASS only when its command exits 0 |
| v2 edition boundary | Required test gate: PASS only when its command exits 0 |
| unsafe lexical and raw-pointer boundary | Required test gate: PASS only when its command exits 0 |
| v2 option rejection | Required test gate: PASS only when its command exits 0 |
| malformed-input crash corpus | Required test gate: PASS only when its command exits 0 |
| deterministic fuzz smoke | Required test gate: PASS only when its command exits 0 |
| hard errors leave no artifact | Required test gate: PASS only when its command exits 0 |
| documentation consistency | Required test gate: PASS only when its command exits 0 |
| generated support matrix | Required test gate: PASS only when its command exits 0 |
| WASM emission regression (no runtime) | Required test gate: PASS only when its command exits 0 |
| GPU frontend validation (not execution) | Required test gate: PASS only when its command exits 0 |
| pointer and memory model | REQUIRED FAILURE: UNSUPPORTED — raw pointer categories and lexical checks exist, but provenance/alignment/lifetime instrumentation is incomplete |
| allocator and reclamation | REQUIRED FAILURE: UNSUPPORTED — no specified v2 allocator API or debug allocator |
| volatile/MMIO | REQUIRED FAILURE: UNSUPPORTED — no language-level volatile operations |
| atomics and concurrency | REQUIRED FAILURE: UNSUPPORTED — no public atomic API or v2 memory model implementation |
| C ABI and dynamic linking | REQUIRED FAILURE: UNSUPPORTED — no bidirectional C ABI/shared-library execution suite |
| CPU intrinsics/SIMD/inline assembly | REQUIRED FAILURE: UNSUPPORTED — no v2 operand/clobber checked asm interface |
| effect adversarial suite | REQUIRED FAILURE: UNSUPPORTED — direct Unsafe propagation is tested, but indirect calls and Atomic/FFI/GPU effects are incomplete |
| physical GPU execution | REQUIRED FAILURE: UNSUPPORTED — no runtime enumerate/allocate/dispatch/readback path |
| sanitizers | REQUIRED FAILURE: PARTIAL — native x86-64 `--sanitize=memory` has bounded ordinary-allocation checks and an exit-time live witness, but no red zones, poisoning, custom allocator coverage, or ABA-resistant identity |
| documentation verification map | REQUIRED FAILURE: UNSUPPORTED — verification matrix exists but records incomplete required capabilities |
