script;

let path: []u8 = "/tmp/zag-script-example.txt";
if (write_file(path, "Zag Script file example\n") != 0) { return 1; }
let contents: []u8 = read_file(path);
if (contents.len < 0) { return 2; }
print(contents);
return 0;
