// Minimal WASM runtime smoke test — compile with:
//   ./znc-target wasm examples/wasm_ret42.zag -o /tmp/wasm_ret42.wasm
// Runtime execution remains unsupported until the runtime is implemented in Zag.

fn main() i32 {
    return 42;
}
