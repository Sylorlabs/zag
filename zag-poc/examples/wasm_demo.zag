// Minimal WebAssembly demo — compile with:
//   ./znc-target wasm examples/wasm_demo.zag -o wasm_demo.wasm

fn add(a: i32, b: i32) i32 {
    return a + b;
}

fn main() i32 {
    let x: i32 = add(40, 2);
    print_i32(x);
    return 0;
}